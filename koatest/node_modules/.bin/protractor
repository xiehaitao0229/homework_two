#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_protractor@5.3.0@protractor/bin/protractor" "$@"
  ret=$?
else 
  node  "$basedir/../_protractor@5.3.0@protractor/bin/protractor" "$@"
  ret=$?
fi
exit $ret
