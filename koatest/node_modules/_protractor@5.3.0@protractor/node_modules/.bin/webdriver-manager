#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_webdriver-manager@12.0.6@webdriver-manager/bin/webdriver-manager" "$@"
  ret=$?
else 
  node  "$basedir/../../../_webdriver-manager@12.0.6@webdriver-manager/bin/webdriver-manager" "$@"
  ret=$?
fi
exit $ret
